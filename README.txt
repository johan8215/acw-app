ACW App v3.6 — Dark Motion Secure

Contenido
- index.html  → App completa (login Google + email+password, semana activa, dashboard).
- config.js   → Config: BACKEND_URL y GOOGLE_CLIENT_ID.
- manifest.json, sw.js, acw-icon.png → PWA lista.
- README.txt.

Cómo subir
1) Sube TODOS los archivos a la raíz del repo de Vercel (no subas la carpeta, sino los archivos).
2) No hace falta build: Framework = Other.
3) Asegúrate de que en el Sheet existe pestaña Employees: A Name, B Password, C Email, D Role, E Phone, F APIKEY.
4) Password por defecto: 0000 (texto).

